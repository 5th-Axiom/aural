# 部署完成（2026-09-09）

入口：https://47.108.226.96

访问密码见本地忽略文件 `.env.test-access.txt`；首次浏览器访问需信任自签名证书。输入密码自动进入共享测试账号。中文候选人样例：https://47.108.226.96/i/eo9060ygaa （先通过首页密码入口，再打开样例）。

服务器发布目录 `/home/deploy/aural-test/current` 指向 `releases/local-ip`，镜像 `aural-test:local-ip`。网页、语音中继、网关及独立 Supabase 均运行。Supabase 已执行 001–005 迁移。

已验证：
- 316 项测试、生产构建成功。
- 服务器上 DeepSeek 实际文字回复、TokenDance 实际音频合成、DashScope 经语音中继识别合成音频。
- 浏览器密码拦截、自动登录仪表盘、Supabase 代理、语音 WSS、HTTPS 安全上下文和模拟麦克风权限。
- 候选人填写信息、创建会话、进入设备检查页。
- 服务端生成 HTTPS 签名文件链接，浏览器实际下载成功。测试文件和两条空会话已删除，保留样例面试。

没有完成真人整场面试、真实摄像头/整屏采集或一小时稳定性测试。需要体验者自行授权硬件；没有邮件服务，使用共享测试账号。

历史问题：首次服务器 Turbopack 构建后 SSH 曾持续超时，现已恢复。后续 `npm run deploy:test` 在本机构建 linux/amd64 镜像，上传后启动，避免服务器编译占用内存。无需重复初始化数据库。

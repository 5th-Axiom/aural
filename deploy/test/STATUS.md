# 部署完成（2026-09-09）

入口：https://47.108.226.96

访问密码见本地忽略文件 `.env.test-access.txt`；首次浏览器访问需信任自签名证书。输入入口密码后进入手机号登录。管理员手机号 `13800138000`，验证码 `123456`；原账号 ID、项目和历史面试保留。新候选人样例：https://47.108.226.96/i/7qwc1kqn7e ，使用手机号 `13800138001`、验证码 `123456` 可体验简历定向题。

服务器发布目录 `/home/deploy/aural-test/current` 指向 `releases/20260909T113333Z-0ac5001-20131`，镜像 `aural-test:20260909T113333Z-0ac5001-20131`。网页、语音中继、网关及独立 Supabase 均运行。Supabase 已执行 001–006 迁移。

发布前已备份数据库到 `/home/deploy/aural-test/backups/pre-phone-resume-20260909.dump`，并验证归档可读取。006 迁移已事务执行，宿主 Nginx 登录路由已更新。

已验证：
- 测试环境真实生成 3 道简历追问；验证候选人题目隔离、会话访问限制、手机号落库、岗位筛选与历史岗位快照。验收用临时会话已清理，保留可体验的样例。
- 327 项本地测试、生产构建成功。
- 服务器上 DeepSeek 实际文字回复、TokenDance 实际音频合成、DashScope 经语音中继识别合成音频。
- 浏览器密码拦截、手机号登录仪表盘、Supabase 代理、语音 WSS、HTTPS 安全上下文和模拟麦克风权限。
- 候选人填写信息、创建会话、进入设备检查页。
- 服务端生成 HTTPS 签名文件链接，浏览器实际下载成功。测试文件和两条空会话已删除，保留样例面试。

没有完成真人整场面试、真实摄像头/整屏采集或一小时稳定性测试。需要体验者自行授权硬件；本期使用模拟短信验证码，不发送真实短信。

历史问题：首次服务器 Turbopack 构建后 SSH 曾持续超时，现已恢复。后续 `npm run deploy:test` 在本机构建 linux/amd64 镜像，上传后启动，避免服务器编译占用内存。无需重复初始化数据库。
